{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase serve --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "configNuxt": "cd.. && cp src/nuxt.config.js functions/nuxt.config.ts && tsc functions/nuxt.config.ts && rm -rf functions/nuxt.config.ts",
    "functionsBuild": "rm -rf functions/nuxt && cd.. && cp -r src/.nuxt/. functions/nuxt/ && cd functions && npm run configNuxt",
    "hostingBuild": "cd.. && rm -rf public/* && cp -r src/static/. public/ && cp -r src/.nuxt/dist/client/ public/_nuxt/ "
  },
  "engines": {
    "node": "8"
  },
  "dependencies": {
    "@nuxtjs/pwa": "^2.6.0",
    "firebase-admin": "^8.1.0",
    "firebase-functions": "^3.0.0",
    "nuxt-start": "^2.8.1"
  },
  "devDependencies": {
    "eslint": "^5.12.0",
    "eslint-plugin-promise": "^4.0.1",
    "firebase-functions-test": "^0.1.6"
  },
  "private": true
}
